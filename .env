TRAEFIK_ENTRYPOINT=<TRAEFIK_ENTRYPOINT> # e.g. trafik.mydomain.com
METRIC_AUTH=<METRIC_AUTH> # auth string like username:hashed-password
PROMETHEUS_ENTRYPOINT=<PROMETHEUS_ENTRYPOINT> e.g. prometheus.mydomain.com
